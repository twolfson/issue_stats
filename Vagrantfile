# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/trusty64"

  # Set up memory since image diffs are CPU intensive
  config.vm.provider "virtualbox" do |v|
    v.memory = 2048
  end

  # sudo apt-get update
  # installed bundler too early
  # added rbenv dependencies
  # ran rbenv commands
  # installed rbenv-bundler
  # installed ruby-build dependnecies
  # install bundler
  # uninstall bundler globally
  # reinstall bundler (finally works)
  # bundle install
  # install capybara-webkit dependencies
  # bundle install
  # sudo apt-get install postgresql-9.3 postgresql-server-dev-9.3 (missing pg dependencies)
  # bundle install

  # https://github.com/nodesource/distributions
  # Install execjs dependencies
  # curl -sL https://deb.nodesource.com/setup_0.10 | sudo -E bash -
  # sudo apt-get install -y nodejs

  # edited /etc/.../pg_hba.conf
  # sudo pico /etc/postgresql/9.3/main/pg_hba.conf
  # restarted postgresql
  # sudo /etc/init.d/postgresql restar
  # however, need to figure out local db setup
end
